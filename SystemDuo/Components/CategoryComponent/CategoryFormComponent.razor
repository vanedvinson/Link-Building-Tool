@inject ISnackbar Snackbar
@inherits CategoryCreateUpdateViewModel

<MudDialog Style="width:600px">
   @* <TitleContent>
        <MudText Typo="Typo.h6">    
            @if (Category != Guid.Empty)
            {
                 Create new category
            }
            else
            {
                Update category
            }
          
        </MudText>
    </TitleContent>*@
    <DialogContent>
      <EditForm Model="@Category" >
        <DataAnnotationsValidator />
        <MudCard Class="m-0" Elevation="0">
            <MudCardContent>
                    <MudText Class="fw-bold mt-2">Name</MudText>
                    <MudTextField Variant="Variant.Outlined" Class="mt-0" Placeholder="Name"
                                  @bind-Value="Category.Name" For="@(() => Category.Name)" />
                   
            </MudCardContent>
          
        </MudCard>
       
       
    </EditForm>
       
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="CreateCategory">Save</MudButton>
    </DialogActions>
</MudDialog>


   

@code 
{
   
 
}